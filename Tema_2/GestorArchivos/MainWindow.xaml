<Window xmlns:Controls="clr-namespace:GestorArchivos.Controls"  x:Class="GestorArchivos.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:viewmodel="clr-namespace:GestorArchivos.ViewModel"
        d:DataContext="{d:DesignInstance Type=viewmodel:MainViewModel}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        >
    <Grid>
        <Grid.RowDefinitions >
            <RowDefinition Height="50"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Controls:HeaderControl Grid.Row="0"/>
        
        <ContentControl Grid.Row="1" Content="{Binding SelectedViewModel}"/>
        <!--<Controls:InfoControl Grid.Row="1"/>-->
    </Grid>
</Window>
